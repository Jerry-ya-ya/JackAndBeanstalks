// import { test, expect } from '@playwright/test';

// test('test', async ({ page }) => {
//   await page.goto('http://localhost:4200');
//   await page.getByRole('link', { name: 'Square' }).click();

//   await expect(page.getByRole('heading', { name: '用戶廣場' })).toBeVisible();
//   await expect(page.getByText('ID:')).toBeVisible();
//   await expect(page.getByText('使用者名稱：')).toBeVisible();
//   await expect(page.getByText('暱稱：')).toBeVisible();
// });